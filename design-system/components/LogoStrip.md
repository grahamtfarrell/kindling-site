# LogoStrip

The trusted-by row: a lead-in label in `ink` ("Trusted by builders at"), then customer names spread across in Syne 500, `ink-2`. It sits at the foot of the hero between corner brackets.

Consumer provides: `label` and `names`. The comps show partner names set as type, so until real partner logos are supplied, set the names in Syne as shown; never approximate a company's logo. Keep partner marks single-colour `ink-2` once real logos arrive.
